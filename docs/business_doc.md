# 📚 Міні-Магазин: Бізнес-Документ

## 1. Назва Системи та Опис (1.a)

Назва: **Mini-Shop (Електронний Каталог Товарів)**.

Це демонстраційна Full Stack веб-застосунок, створена для відображення каталогу товарів, що зберігаються у базі даних SQLite. Система демонструє базову трирівневу архітектуру (Client → Backend → Database) і слугує основою для подальшого розширення до повноцінного інтернет-магазину.

## 2. Користувачі (Ролі) (1.b)

* **🛒 Покупець (Client/Guest):** Переглядає доступні товари та їхні ціни.
* **🧑‍💻 Адміністратор (API Consumer):** Взаємодіє з бекенд API для управління каталогом (додавання, редагування, видалення товарів).

## 3. Основні Сценарії Використання (1.c)

1.  **Перегляд Каталогу:** Покупець відкриває головну сторінку та бачить актуальний список товарів.
2.  **Отримання Даних API:** Адміністратор робить GET-запит до `/items` для отримання повного каталогу товарів.
3.  **Додавання Товару (Майбутнє):** Адміністратор робить POST-запит для внесення нового товару в базу даних.
4.  **Зміна Ціни (Майбутнє):** Адміністратор робить PUT/PATCH-запит для оновлення ціни існуючого товару.

## 4. Функціональні Вимоги (1.d)

| ID | Опис Вимоги |
| :--- | :--- |
| FR1 | Система повинна відображати список усіх товарів (ID, назва, ціна) на клієнтській сторінці. |
| FR2 | Бекенд повинен надавати API-endpoint `/items` для отримання списку товарів у форматі JSON. |
| FR3 | Бекенд повинен підключатися до БД SQLite та виконувати SQL-запит для отримання даних. |
| **FR4** | **Система повинна надавати функціонал CRUD (Create, Read, Update, Delete) для управління товарами (Майбутнє).** |

## 5. Нефункціональні Вимоги (1.e)

* **NF1. Швидкодія:** Час відгуку API на запит `/items` не повинен перевищувати **300 мс**.
* **NF2. Зручність (UX):** Інтерфейс має бути зрозумілим, з чітким відображенням ціни та назви.
* **NF3. Надійність:** Бекенд повинен мати коректну обробку помилок з’єднання з БД.

## 6. Обмеження (1.f)

* Система **не буде** обробляти платежі чи транзакції у найближчому майбутньому.
* Система **не буде** містити повноцінну систему кошика.
* Система **не буде** реалізовувати складні бізнес-правила (знижки, логістику).